Self-signed SSL certificates are generated locally and should not be committed.

Generate them with:

```bash
npm run certs:generate
```

Default files:
- `key.pem` — private key
- `cert.pem` — certificate
