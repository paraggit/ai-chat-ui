# Hugging Face API token (optional if set in the UI)
# Create a fine-grained token with "Make calls to Inference Providers" at:
# https://huggingface.co/settings/tokens
HF_TOKEN=

# Model ID on Hugging Face Hub
# Examples: Qwen/Qwen2.5-7B-Instruct, meta-llama/Llama-2-7b-chat-hf
HF_MODEL=Qwen/Qwen2.5-7B-Instruct

# Request timeout in milliseconds (10–20s for router, up to 180s for custom endpoints)
HF_TIMEOUT_MS=15000
HF_ENDPOINT_TIMEOUT_MS=180000

# Max tokens in model replies (512 was too low — reasoning models need more headroom)
HF_MAX_TOKENS=4096

# Vision model for image uploads (optional)
HF_VISION_MODEL=Salesforce/blip-vqa-base

# Text-to-image model for generated responses (optional)
HF_IMAGE_GEN_MODEL=stabilityai/stable-diffusion-2-1

# Custom Inference Endpoint base URL (optional)
# Example: https://abc123.us-east-1.aws.endpoints.huggingface.cloud
# HF_API_BASE=https://your-endpoint.aws.endpoints.huggingface.cloud

# Local Llama / Ollama (optional server-side defaults; UI "Local Llama" mode overrides per request)
# LLM_PROVIDER=local
# LOCAL_LLM_URL=http://localhost:11434
# LOCAL_LLM_MODEL=llama3.2
# LOCAL_LLM_TIMEOUT_MS=120000

# Conversation context & memory (token counts are approximate)
CONTEXT_MAX_RECENT_MESSAGES=16
CONTEXT_MAX_TOKENS=8192
CONTEXT_SUMMARIZE_THRESHOLD=20
CONTEXT_SUMMARY_MAX_TOKENS=400
CONTEXT_LONG_TERM_MAX_FACTS=20

# Server
PORT=3001
NODE_ENV=development

# Self-signed SSL (optional)
# Generate certs: npm run certs:generate
# SSL_ENABLED=true
# SSL_KEY_PATH=./certs/key.pem
# SSL_CERT_PATH=./certs/cert.pem

# Allowed frontend origin (CORS)
# HTTP:  CLIENT_ORIGIN=http://localhost:5173
# HTTPS: CLIENT_ORIGIN=https://localhost:5173
CLIENT_ORIGIN=http://localhost:5173
